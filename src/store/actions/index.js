// This is the file from where we export all the actions
// helpful in a way as we do not need to import different action files
// but only the index.js
export { addPlace, deletePlace } from './places'
export { tryAuth } from './auth';